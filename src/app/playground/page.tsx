import PlaygroundDemo from "../../../playground/PlaygroundDemo";

export const metadata = {
  title: "Accessibility Playground | W3C ARIA Handcrafted Components",
  description: "Keyboard-only interactive testing for ModalDialog (focus trap & restoration), Tabs (roving tabindex), and Disclosure.",
};

export default function PlaygroundPage() {
  return <PlaygroundDemo />;
}
